## A Discord bot, written in Go, that you can invite to your server that fetches IP info from abstractAPI.

### Usage:

#### Run the bot locally or in hosted environment:
``` go run bot.go --token <your_API_token> --guild <server_guild> ```

#### In Discord server
``` /findme ip: <ipaddress> ```


